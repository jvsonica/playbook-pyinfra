[project]
name = "playbook-pyinfra"
version = "0.1.0"
description = "Pyinfra playbooks for workstation setup."
requires-python = ">=3.11"
dependencies = ["pyinfra>=3.0"]

[dependency-groups]
dev = ["ruff>=0.15.1"]

[tool.ruff]
target-version = "py311"
